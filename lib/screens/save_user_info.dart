import 'package:flutter/material.dart';

class SaveUserInfo extends StatelessWidget {
  const SaveUserInfo({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
       appBar: AppBar(
         title: Text('User Profile'),
       ),
    );
  }
}
